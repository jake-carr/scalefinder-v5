import React, { Component } from 'react'
// this file is a placeholder, delete once other components are done and imported to app/

// TODO

// Metronome may need changes to work on mobile
// Local storage mobile compatible? What about React-tooltip, React-slider, React-dropdown?
// String stepper capped at 6 instead of 8
// No fret stepper, just 12 frets
// Not sure how well tuner modals will work - just use +/- buttons for them if not
// Locked Horizontal/Landscape Layout

export default class MobileLayout extends Component {
  render() {
    return <div>mobile layout</div>
  }
}
